# Voice AI agent — Python deps. Install torch separately (CPU wheel) via `make install`.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
anthropic>=0.40
faster-whisper>=1.0.3
silero-vad>=5.1
kokoro>=0.9.2
# Kokoro 0.9.x imports transformers.AlbertModel, which transformers 5.x removed/relocated.
# Pin to the 4.x line or Kokoro fails to load (silently falls back to Piper). Floor raised
# to 4.51 for the Phase 7 SLM probe (Qwen3 chat template + tool-calling support); 4.51 still
# has AlbertModel so Kokoro is unaffected.
transformers>=4.51,<5
# Phase 7 SLM probe (scripts/slm_probe.py): accelerate for device_map load, bitsandbytes
# for the 4-bit quantized path (--dtype 4bit, the ~3.4GB production footprint).
accelerate>=0.30
bitsandbytes>=0.43
piper-tts>=1.3.0
soundfile>=0.12
scipy>=1.11
numpy>=1.26,<2.3
python-dotenv>=1.0
httpx>=0.27
websockets>=12.0
pynacl>=1.5  # Ed25519 verification of Telnyx webhook signatures
# Phase 4: Google Workspace adapters (Sheets CRM + Calendar demos)
google-api-python-client>=2.100
google-auth>=2.30
